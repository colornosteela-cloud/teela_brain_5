# Teela Brain 5 — Python Dependencies
# ==================================
# Install with: pip install -r requirements.txt

# ── Core Scientific ──
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
scipy>=1.11.0

# ── ML / Vision ──
torch>=2.1.0
torchvision>=0.16.0
onnx>=1.15.0
transformers>=4.36.0

# ── Perception ──
mediapipe>=0.10.0          # Human pose + pointing detection
# vjepa (optional, slow on Jetson): pip install vjepa

# ── Communication ──
pyserial>=3.5              # Teensy USB Serial
websockets>=12.0           # Optional WebSocket bridge

# ── Voice / Audio ──
sounddevice>=0.4           # Live microphone capture (install if using real mic)
edge-tts>=6.0              # Cloud TTS — Microsoft edge voices, zero GPU load
# pvporcupine>=3.0         # Wake word "Teela" — picovoice.ai, free for open-source
# openwakeword             # Open-source wake word — ONNX, ~50MB RAM
# faster-whisper           # Local STT — runs on Jetson Orin Nano GPU
# openai-whisper           # Alternative: OpenAI Whisper (heavier)
# pyttsx3                  # Offline TTS — robotic but no network
# espeak                   # Linux offline TTS via aplay

# ── Config / Utils ──
pyyaml>=6.0
schedule>=1.2.0
requests>=2.31.0

# ── Dev / Testing ──
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.7.0
